# 10 — 完成真实储值任务接口

**What to build:** 在 Windows 环境交付真实储值任务创建与完成接口，使已成功授权且没有未完成任务的墨盒可以获得固定增量，并由回读余额完成验收。

**Blocked by:** 09 — 走通真实墨盒授权

**Status:** ready-for-agent

- [ ] 创建接口只接受已成功授权且无未完成储值任务的 UID。
- [ ] 创建响应包含任务数据和每卡管理密钥，不返回客户密钥。
- [ ] 完成接口校验身份元数据、认证结果、前后余额和指定增量。
- [ ] 同一 `task_id` 重复完成返回原结果，不生成重复储值记录。
- [ ] 结果不确定的未完成任务会阻止该 UID 再次创建储值任务。
