# RFID M1 授权加密桌面端 UI 原型（A 方案）

> 已采用 A「引导工作台」方案：授权与储值流程和客户管理置于同一桌面端框架中。

```bash
npm install
npm run dev
```

打开终端给出的地址，例如 `http://localhost:5173/`。

- 所有登录、客户、密钥材料、操作与读卡器状态都仅为内存 Mock。
- 原型不含 Electron、真实串口、后端调用、令牌或任何密钥材料。
- 当前 Mock 状态始终可见；工作台与客户管理可在左侧切换。
